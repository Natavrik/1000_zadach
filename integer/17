Дано целое число, большее 999.
Используя одну операцию деления нацело 
и одну операцию взятия остатка от деления,
найти цифру, соответствующую разряду сотен
в записи этого числа.



#include <stdio.h>
int main (){
    int a;
    scanf("%d", &a);
    if (a>999)
    {
        a/=100;
        a%=10;
        printf("%d\n", a);
    }else 
{
    printf("всё по новой");
}
    return 0;
}
