/*    8. Даны два целых числа: A, B. 
Проверить истинность высказывания:
«Каждое из чисел A и B нечетное   */

#include<stdio.h>
int main (){
      int a,b,bybyby,q=1,w=0;
      printf("'a' и 'b'  нужно проверить на нечётность\nтак давайте сделаем это!\nвведите эти числа через ',': ");
      scanf("%d%d",&a,&b);
      if ((a%2==1)&&(b%2==1))
      bybyby=1;
      else
      bybyby=w;
      printf("1 - истина\n0 - ложь\nответ: %d", bybyby);
   return 0;
}
