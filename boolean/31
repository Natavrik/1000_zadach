/* 31. Даны целые числа a, b, c, 
являющиеся сторонами некоторого треугольника.
Проверить истинность высказывания:
«Треугольник со сторонами a, b, c является равнобедренным». */


#include<stdio.h>
    int main (){
    int a, b, c, k, d, by, q=1, w=0;
        printf(" треугольник является равнобедренным, если стороны основания  bc равны.\n");
        printf("___________________________________________________________________________\n");
        printf("___________________________________________________________________________\n\n");
        printf(" Треугольник со сторонами a, b, c является равнобедренным?\n проверим \n введите высоты сторон a, b, c: ");
        scanf("%d%d%d",&a, &b, &c);
        if ( b==c )
        by=q;
        else
        by=w; 
        printf("        1 - истина\n        0 - ложь\n        ответ: %d", by);
    return 0;
}
