/* 32. Даны целые числа a, b, c,
являющиеся сторонами некоторого треугольника. 
Проверить истинность высказывания: 
«Треугольник со сторонами a, b, c является прямоугольным». */


#include<stdio.h>
    int main (){
    int a, b, c, k, d, by, q=1, w=0;
        printf(" Проверяем истинность теоремы Пифагора для каждой тройки чисел:  \n");
        printf("___________________________________________________________________________\n");
        printf("___________________________________________________________________________\n\n");
        printf(" Треугольник со сторонами a, b, c является прямоугольным?\n проверим \n введите высоты сторон a, b, c: ");
        scanf("%d%d%d",&a, &b, &c);
        if ((pow(a,2)==pow(b,2)+pow(c,2)) || (pow(b,2)==pow(a,2)+pow(c,2)) || (pow(c,2)==pow(a,2)+pow(b,2)) )
        by=q;
        else
        by=w; 
        printf("        1 - истина\n        0 - ложь\n        ответ: %d", by);
    return 0;
}
