/*   10. Даны два целых числа: A, B. 
Проверить истинность высказывания: 
«Ровно одно из чисел A и B нечетное».    */

#include<stdio.h>
int main (){
      int a,b,bybyby,q=1,w=0,sum;
      printf("Среди двух чисел A и B нечетным будет одно\n тогда и только тогда,\n когда их сумма нечетна. \nвведите эти числа через ',': ");
      scanf("%d%d",&a,&b);
      sum=a+b;
      if (sum%2==1)
      bybyby=q;
      else
      bybyby=w;
      printf("1 - истина\n0 - ложь\nответ: %d", bybyby);
   return 0;
}
