/* 25. Даны числа x, y. 
Проверить истинность высказывания:
«Точка с координатами (x, y)
лежит во второй координатной четверти». */


#include<stdio.h>
    int main (){
    int x, y, by, q=1, w=0;
        printf("Точка с координатами (x, y) лежит во второй координатной четверти?\nпроверим \nвведите число: ");
        scanf("%d%d",&x,&y);
        if ((x<0)&&(y>0))
        by=q;
        else
        by=w;
        printf("        1 - истина\n        0 - ложь\n        ответ: %d", by);
    return 0;
}
