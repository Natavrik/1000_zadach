/* 29. Даны числа x, y, x1, y1, x2, y2. 
Проверить истинность высказывания:
«Точка с координатами (x, y) 
лежит внутри прямоугольника,
левая верхняя вершина которого
имеет координаты (x1, y1),
правая нижняя — (x2, y2),
а стороны параллельны координатным осям». */


#include<stdio.h>
    int main (){
    int x, y, x1, x2, y1, y2, by, q=1, w=0;
        printf(" Точка с координатами (x, y) лежит внутри прямоугольника, \n левая верхняя вершина которогоимеет координаты (x1, y1), \n правая нижняя — (x2, y2),\n а стороны параллельны координатным осям?\nпроверим \nвведите координаты x, y: ");
        scanf("%d%d",&x,&y);
        printf("введите границы параллелограмма x1, x2, y1, y2: ");
        scanf("%d%d%d%d", &x1, &x2, &y1, &y2);
        if ( (x>x1)&&(x<x2)&&(y<y1)&&(y>y2) )
        by=q;
        else
        by=w; 
        printf("        1 - истина\n        0 - ложь\n        ответ: %d", by);
    return 0;
}
