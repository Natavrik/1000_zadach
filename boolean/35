/* 
35. Даны координаты двух различных полей шахматной доски
x1, y1, x2, y2 (целые числа, лежащие в диапазоне 1–8). 
Проверить истинность высказывания:
«Данные поля имеют одинаковый цвет». 
*/


#include<stdio.h>
    int main (){
    int x1, y1, x2, y2, s1, s2, q, w, e, r, by, p=1, o=0;
        printf(" \n");
        printf("___________________________________________________________________________\n");
        printf("___________________________________________________________________________\n\n");
        printf(" Данные поля имеют одинаковый цвет?\n проверим \n введите координаты клетки поля x1 и y1: ");
        scanf("%d%d",&x1, &y1);
        printf("введите координаты клетки поля x и y: "); 
        scanf("%d%d",&x2, &y2);
        s1=x1+y1;
        s2=x2+y2;
        q=((s1)/2==1);
        w=((s2)/2==1);
        e=((s1)/2==0);
        r=((s2)/2==0);
        if (   ((x1>=1)&&(x1<=8)&&(y1>=1)&&(y1<=8))  &&  ((x2>=1)&&(x2<=8)&&(y2>=1)&&(y2<=8))   &&   ((q==w)||(e==r))  )
        by=p;
        else
        by=o; 
        printf("        1 - истина\n        0 - ложь\n        ответ: %d", by);
    return 0;
}
