/*  34. Даны координаты поля шахматной доски x, y 
(целые числа, лежащие в диапазоне 1–8).
Учитывая, что левое нижнее поле доски (1, 1) является черным, 
проверить истинность высказывания: «Данное поле является белым».  */


#include<stdio.h>
    int main (){
    int x, y, by, q=1, w=0;
        printf(" учитывайте, что поле размером 8х8 \n");
        printf(" чтобы поле было белым, необходимо и достаточно,\n");
        printf(" чтобы сумма координат была нечетна, \n");
        printf(" то есть при делении на 2 давала в остатке единицу.\n");
        printf("___________________________________________________________________________\n");
        printf("___________________________________________________________________________\n\n");
        printf(" Данное поле является белым?\n проверим \n введите координаты клетки поля x и y: ");
        scanf("%d%d",&x, &y);
        if ((x>=1)&&(x<=8)&&(y>=1)&&(y<=8)&&((x+y)/2==1))
        by=q;
        else
        by=w; 
        printf("        1 - истина\n        0 - ложь\n        ответ: %d", by);
    return 0;
}
