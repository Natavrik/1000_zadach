/* 24. Даны числа A, B, C (число A не равно 0).
Рассмотрев дискриминант D = B2 − 4·A·C,
проверить истинность высказывания: 
«Квадратное уравнение A·x2 + B·x + C = 0 имеет вещественные корни». */


#include<stdio.h>
    int main (){
    int A, B, C, D,  bybyby, by, q=1, w=0;
        printf("Квадратное уравнение A·x2 + B·x + C = 0 имеет вещественные корни?\nпроверим \nвведите число: ");
        scanf("%d%d%d",&A,&B,&C);
        D = pow(B,2) - 4*A*C;
        if (D > 0)
        by=q;
        else
        by=w;
        printf("        1 - истина\n        0 - ложь\n        ответ: %d", by);
    return 0;
}
