/* 
3. Дано целое число. 
Если оно является положительным, то прибавить к нему 1; 
если отрицательным, то вычесть из него 2;
если нулевым, то заменить его на 10.
Вывести полученное число.

*/


#include<stdio.h>
#include<math.h>
    int main (){
    int N;
                    printf("Введите желаемое число: ");
                    scanf("%d", &N);
                    if (N>0)
                    N+=1;
                    if (N<0)
                    N-=2;
                    if (N==0)
                    N=10;
                    printf("        Ваш ответ: %d", N);
    return 0;
}



