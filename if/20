/*
20. На числовой оси расположены три точки: A, B, C.
Определить, какая из двух последних точек (B или C)
расположена ближе к A, 
и вывести эту точку 
и ее расстояние от точки A.

*/

#include<stdio.h>
int main () {
    int a, b, c, r;

                printf(" какая из двух последних точек расположена ближе к точке <a> \n вывести эту точку и её расстояние\n\n");
                printf(" введите желаемые три точки: ");
                 scanf("%d%d%d", &a, &b, &c);
                 
                if ((a<b)&&(a<c)&&(b<c)) {
                    r=b-a;
                    printf(" ближняя точка и её расстояние: %d; %d", b, r);
                }
                
                if ((a>b)&&(a>c)&&(b<c)) {
                    r=c-a;
                    printf(" ближняя точка и её расстояние: %d; %d", c, r);
                }
                
                if ((a>b)&&(a>c)&&(b>c)) {
                    r=b-a;
                    printf(" ближняя точка и её расстояние: %d; %d", b, r);
                }
                
                if ((a<c)&&(a<b)&&(b>c)) {
                    r=c-a;
                    printf(" ближняя точка и её расстояние: %d; %d", c, r);
                }
                
                if ((a>b)&&(a<c)) {
                    if (b>c) {
                        r=b-a;
                        printf(" ближняя точка и её расстояние: %d; %d", b, r);
                    }
                    else {
                        r=c-a;
                    printf(" ближняя точка и её расстояние: %d; %d", c, r);
                    }
                }
                
}
