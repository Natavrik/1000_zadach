/*
10. Даны две переменные целого типа: A и B.
Если их значения не равны,
то присвоить каждой переменной сумму этих значений,
а если равны,
то присвоить переменным нулевые значения. 
Вывести новые значения переменных A и B.

*/

int main () {
    int a, b, s=0;
    
                printf("если их значения не равны, то каждой переменной присваивается их сумма\n");
                printf("если значения равны, то присвоить переменным значения равными нулю\n\n");
                printf("введите желаемые два числа: ");
                scanf("%d%d",&a, &b);
                s=a+b;
                if (a != b){
                    a=s;
                    b=s;
                    printf("Ваш ответ: %d, %d\n", a, b);
                }
                else {
                    a=0;
                    b=0;
                    printf("Ваш ответ: %d, %d", a, b);
                }
}
