/* 
5. Даны три целых числа. 
Найти количество положительных и количество отрицательных чисел в исходном наборе.

*/


#include<stdio.h>
#include<math.h>
    int main (){
    int a, b, c, pol=0, otr=0;
    
                    printf("введите три числа, что бы узнать, сколько из них положительных, а сколько отрицательных: ");
                    scanf("%d%d%d", &a,&b,&c);
                    if (a>0)
                    pol++;
                     if (a<0)
                    otr++;
                    if (b>0)
                    pol++;
                    if (b<0)
                    otr++;
                    if (c>0) 
                    pol++;
                    if (c<0) 
                    otr++;
                    printf(" Ваш ответ:\n кол-во положительных: %d\n", pol );
                    printf(" кол-во отрицательных: %d", otr);
                    
    return 0;
}
